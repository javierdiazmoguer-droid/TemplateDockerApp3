<IfModule status_module>
    ExtendedStatus On

    <Location "/status">
        SetHandler server-status
        Require all granted

        # Para permitir acceso desde tu red interna:
        # Require ip 192.168.1.0/24
    </Location>
</IfModule>
